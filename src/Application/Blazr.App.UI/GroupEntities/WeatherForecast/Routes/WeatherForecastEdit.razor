@page "/WeatherForecast/Edit/{Uid:guid}"
@namespace Blazr.App.UI

<WeatherForecastEditForm Uid=this.Uid />
@code {

    [Parameter] public Guid Uid { get; set; }

    private static Guid RouteId = Guid.NewGuid();
}
